name: Test renderer
description: Test renderer

runs:
  using: 'composite'
  steps:
    - name: Prepare renderer env
      uses: ./.github/workflows/modules/renderer/setup-env

    - name: Build renderer tests with wm
      shell: bash
      run: |
        export CC=${{ env.cc }}
        export CXX=${{ env.cxx }}
        wm run test -wd
